# 表设计与变更记录

## 文档头

- 业务名称：
- 功能点：
- 当前目标数据库：
- 数据库判断依据：
- 对应 profile / 配置：
- 文档维护时间：

## 一、业务涉及表总览

| 表名 | 表中文名称 | 表用途 | 首次创建时间 | 当前状态 | 逻辑关联摘要 | 当前维护状态 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

## 二、变更流水总览

说明：

- 一条变更一行
- 按时间倒序记录
- `变更时间` 使用 `YYYY-MM-DD HH:mm`
- `时段标签` 固定使用：`凌晨`、`早上`、`上午`、`中午`、`下午`、`晚上`、`夜间`
- `变更动作` 建议使用统一枚举：`创建表`、`新增字段`、`修改字段`、`删除字段`、`新增索引`、`删除索引`、`调整注释`、`数据回填`、`兼容迁移`

| 变更编号 | 变更时间 | 时段标签 | 表名 | 变更动作 | 变更摘要 | 影响对象 | SQL锚点 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |

## 三、单表详细变更记录

### 表名

#### 1. 表基础信息

- 表中文名称：
- 表用途：
- 首次创建时间：
- 当前状态：
- 当前维护状态：
- 逻辑关联摘要：

#### 2. 当前字段清单

| 字段名 | 中文名称 | 类型/长度 | 是否允许为空 | 默认值 | 字段注释 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

#### 3. 当前索引清单

| 索引名 | 索引类型 | 索引字段 | 用途说明 |
| --- | --- | --- | --- |
|  |  |  |  |

#### 4. 变更历史

| 变更编号 | 变更时间 | 时段标签 | 变更动作 | 变更内容 | 影响对象 | 兼容性说明 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |

#### 5. SQL脚本明细

##### 5.1 建表 SQL

```sql
-- 按当前项目实际数据库方言填写
-- 本次无则保留：-- 本次无
```

##### 5.2 字段 / 索引变更 SQL

```sql
-- 按当前项目实际数据库方言填写
-- 本次无则保留：-- 本次无
```

##### 5.3 注释 / 逻辑关联补充 SQL

```sql
-- 按当前项目实际数据库方言填写
-- 本次无则保留：-- 本次无
```

##### 5.4 数据回填 / 兼容迁移 SQL

```sql
-- 按当前项目实际数据库方言填写
-- 本次无则保留：-- 本次无
```

## 四、附加说明

### 1. 其他数据库脚本同步情况

- 是否同步更新 MySQL 脚本：
- 是否同步更新 Oracle 脚本：
- 是否同步更新 PostgreSQL 脚本：
- 未同步原因：

### 2. 受影响代码或 SQL

- 

### 3. 风险与兼容性说明

- 

### 4. 后续清理项

- 

### 5. 备注

- 
