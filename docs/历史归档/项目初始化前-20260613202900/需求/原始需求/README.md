# 原始需求

本目录只存放作为 AI 正式输入的批次化需求资料。

## 使用规则

1. 每一批需求使用独立目录，目录名建议采用 `<YYYYMMDD>批次需求`。
2. 每个批次目录至少包含一份 `批次说明.md`，用于给 AI 提供变更类型判定锚点。
3. 正式需求拆分时，优先读取本目录和 `docs/原型/` 下的同批次目录，再决定是否复用已有模块、是否新建需求包。

## 当前批次

| 批次目录 | 对应原型 | 当前说明 |
| --- | --- | --- |
| 暂无 |  | 演示需求批次已清理 |
