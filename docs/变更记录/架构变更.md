# 架构变更记录

## 2026-06-14 — 需求开发阶段变更

### opinion 模块扩展
- 新增专业反馈（dept-feedback）和专业审核（dept-audit）子模块
- 新增进度查询（progress）和提醒（reminder）子模块
- 新增 `ad_opinion_feedback.remark` 字段
- FeedbackRepository 改用 JDBC 直连（绕过 MyBatis-Plus selectList 兼容问题）

### 数据库变更
- `ad_opinion_feedback` 增加 `remark VARCHAR(500)`

## 2026-06-14 — 业务模块重命名

- 业务应用模块由 `aldemo` 重命名为 `aldemohk`
- 物理目录、artifactId、spring.application.name、Nacos dataId 同步收敛
- 依赖方向保持不变：backend-api ← backend-core ← backend-data ← aldemohk
- 共享模块 backend-api/backend-core/backend-data 名称不变
