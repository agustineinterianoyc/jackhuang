# 需求实现记录 — 指标体系意见征集

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 模块编码 | `opinion` |
| 需求包 | 2026-06-opinion-v2.1 |
| 设计基线 | `docs/设计/指标体系意见征集模块/`（v1.0） |
| 开始日期 | 2026-06-14 |
| 当前状态 | 开发中 |

## 实现记录

### STEP-001 数据库基线 — ✅ 已完成

- **实现日期**：2026-06-14
- **关键产出**：
  - `code/backend/aldemohk/src/main/resources/db/mysql/schema.sql`（13 张 `ad_opinion_*` 表 DDL）
  - 13 个枚举常量类（`OpinionMainStatus`、`OpinionUnitFillStatus`、`OpinionUnitAuditStatus`、`OpinionDeptSubmitStatus`、`OpinionDeptAuditStatus`、`OpinionModuleCode`、`OpinionUnitType`、`OpinionCategory`、`OpinionAuditAction`、`OpinionAttachmentBizType`、`OpinionReminderTargetType`、`OpinionReminderMode`、`OpinionActionLogAction`）
  - 13 个 DO：`code/backend/aldemohk/src/main/java/com/hk/demo/app/model/dataobject/opinion/`
  - 13 个 Mapper：`code/backend/aldemohk/src/main/java/com/hk/demo/app/mapper/opinion/`
  - 数据库变更记录：OPINION-DDL-001
- **验证结果**：`./mvnw -pl aldemohk -am -DskipTests compile` ✅；本地 MySQL `SHOW TABLES LIKE 'ad_opinion%'` 返回 13 张表

### STEP-002 公共能力 — ✅ 已完成

- **实现日期**：2026-06-14
- **关键产出**：
  - `OpinionStateMachine`（状态机单例） + `OpinionStateMachineTest`（11/11 通过）
  - `OpinionActionLogger`（审计日志写入）
  - `OpinionMockMasterDataProvider`（模拟主数据提供器）
  - `OpinionDictController`（字典查询 Mock）
  - `OpinionResultCode` 错误码扩展
- **验证结果**：状态机单元测试全部通过；权限切面可用

### STEP-003 P01 主链路 — ✅ 已完成

- **实现日期**：2026-06-14
- **关键产出**：
  - `OpinionSurveyController` + `OpinionSurveyService`
  - API-101 创建征集
  - API-102 编辑征集
  - API-103 删除征集
  - API-108 列表查询
  - API-109 详情查询
  - API-104 开启征集（含基层任务初始化）
- **验证结果**：P01 创建→编辑→开启流程可走通

### STEP-004 基层链路 — ✅ 已完成

- **实现日期**：2026-06-14
- **关键产出**：
  - `OpinionUnitFillController` + `OpinionUnitFillService`
  - `OpinionUnitAuditController` + `OpinionUnitAuditService`
  - API-201~204（基层填报 CRUD + 提交）
  - API-301~304（基层审核 CRUD + 通过/退回）
  - `OpinionAutoZeroReportJob`（零报送定时任务）
  - OPINION-DDL-002：`ad_opinion_item.reason` 改为可空
- **验证结果**：P02/P03 填报→提交→审核通过/退回闭环通过；零报送按预期执行

### STEP-005 专业链路 — ❌ 待实现

- **计划产出**：
  - `OpinionDeptFeedbackController/Service`
  - `OpinionDeptAuditController/Service`
  - API-105、111、401~404、501~504
- **前置依赖**：STEP-004 完成；部门 ↔ 模块映射可用

### STEP-006 进度查询与提醒 — ❌ 待实现

- **计划产出**：
  - `OpinionProgressController/Service`
  - `OpinionReminderController/Service`
  - API-601~604
- **前置依赖**：STEP-004、STEP-005

### STEP-007 汇总发布 — ❌ 待实现

- **计划产出**：
  - `OpinionSummaryController/Service`
  - API-106、107、701~704
- **前置依赖**：STEP-005、STEP-006

### STEP-008 专业截止提醒 — ⚠️ 部分完成

- **已完成**：零报送（`OpinionAutoZeroReportJob`）
- **待实现**：专业截止提醒（`OpinionDeptDeadlineRemindJob`）
- **前置依赖**：STEP-005

### STEP-009 导入/导出补完 — ❌ 待实现

- **计划产出**：API-801、802
- **前置依赖**：STEP-007 导出能力就绪

## 变更记录索引

| 变更编号 | 类型 | 日期 | 说明 |
| --- | --- | --- | --- |
| OPINION-DDL-001 | 新增表 | 2026-06-14 | 首次落地 13 张表 |
| OPINION-DDL-002 | 字段变更 | 2026-06-14 | `ad_opinion_item.reason` 改为可空 |

详见 `code/backend/DataSQL/opinion/业务变更明细.md`
