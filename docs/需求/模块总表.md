# 模块总表

`模块总表.md` 是项目业务模块命名的唯一事实源。

新增需求包、后端代码目录、测试目录和 DataSQL 目录前，先查本表。

| 模块中文名 | 模块编码 | 后端目录名 | DataSQL目录名 | 主要需求包 | 涉及原型 | 当前状态 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 指标体系意见征集 | `opinion` | `opinion` | 指标体系意见征集 | `2026-06-opinion-baseline` | `20260613-opinion-v2.1` | 待开始 | 8 个子模块（survey/unit-fill/unit-audit/dept-feedback/dept-audit/progress/summary/attachment）；表前缀 `ad_opinion_`；接口前缀 `/api/opinion/` |

## 维护规则

1. 模块编码确定后跨需求包保持稳定。
2. 新需求命中已有模块时，继续复用原模块编码。
3. 后端包目录、Mapper XML 目录、测试目录优先使用模块编码。
4. DataSQL 目录名优先使用模块中文名，并在本表中登记。
5. 模块拆分有争议时，先在说明中记录暂定原因，后续复核时再调整。
