# 实现计划 — 2026-06-opinion-v2.1

## AI 判定结果

- **设计基线引用**：`docs/设计/指标体系意见征集模块/`（4 份设计文档 v1.0）
- **模块编码**：`opinion`
- **判定依据**：基于已评审通过的设计说明书、数据库设计说明书、接口清单和需求实现计划

## 模块拆分结论

| 模块名称 | 模块编码 | 本轮职责 | 影响页面 / 菜单 | 相关接口 | 相关数据 |
| --- | --- | --- | --- | --- | --- |
| 意见征集管理 | `opinion.survey` | 任务 CRUD、开启、推进、汇总、发布 | P01 全部 | API-101 ~ 111 | `ad_opinion_survey/_module/_target/_attachment` |
| 基层意见填报 | `opinion.unit-fill` | 基层填报 / 提交 / 查看 | P02 | API-201 ~ 204、API-801 | `ad_opinion_unit_task/_item` |
| 基层意见审核 | `opinion.unit-audit` | 基层审核 / 退回 | P03 | API-301 ~ 304 | `ad_opinion_unit_task/_unit_audit_log` |
| 专业部门反馈 | `opinion.dept-feedback` | 专业反馈 / 提交 | P04 | API-401 ~ 404、API-802 | `ad_opinion_dept_task/_feedback` |
| 专业部门反馈审核 | `opinion.dept-audit` | 专业审核 / 退回 | P05 | API-501 ~ 504 | `ad_opinion_dept_task/_dept_audit_log` |
| 进度查询与提醒 | `opinion.progress` | 进度弹窗 + 一键 / 单条提醒 | P01 弹窗 | API-601 ~ 604 | `ad_opinion_unit_task/_dept_task/_reminder_log` |
| 汇总发布 | `opinion.summary` | 汇总采纳 / 调整后内容 / 发布 / 报表导出 / 模板导入 | P01 汇总页 | API-106、107、701 ~ 704 | `ad_opinion_summary_item` |
| 附件管理 | `opinion.attachment` | 模块附件 / 汇总附件挂载 | 跨页面 | API-901、902 | `ad_opinion_attachment` |

## 分步骤实现计划

### STEP-001 数据库基线

- **步骤目标**：完成 13 张表的 DDL、初始留痕、枚举常量、基础 DO/Mapper
- **影响面**：[后端] [数据库] [文档]
- **关联接口**：无
- **关联数据表**：`ad_opinion_*` 全部 13 张
- **后端任务概览**：
  - 落 `code/backend/aldemohk/src/main/resources/db/mysql/schema.sql`
  - 在 `code/backend/DataSQL/opinion/` 建立业务子目录与表设计文档
  - 创建 13 个枚举常量类（`OpinionMainStatus` 等）
  - 生成 13 个 DO + 13 个 Mapper（不含业务方法）
- **前端任务概览**：无
- **数据库任务概览**：建表、索引、字符集 `utf8mb4`
- **验证方式**：`./mvnw -pl aldemohk -am -DskipTests validate`；本地启动后表自动建立成功
- **验收口径**：库内 13 张表存在；DataSQL 留痕完整；枚举常量类编译通过
- **实现状态**：✅ 已完成

### STEP-002 公共能力（状态机、权限、响应包装、错误码）

- **步骤目标**：把跨模块共享的状态机、权限切面、统一响应、错误码骨架补齐；前端三栏枚举映射可用
- **影响面**：[前端] [后端]
- **关联接口**：无（基础设施）
- **关联数据表**：`ad_opinion_action_log`
- **后端任务概览**：
  - `OpinionStateMachine`：所有状态推进、退回、零报送动作收口
  - `OpinionDataScope` 切面：按角色 + `unit_id` / `department_id` 隔离
  - `OpinionResultCode` 扩展 `ResultCode`，新增 `OPINION_*` 业务码
  - `OpinionActionLogger` 写入 `ad_opinion_action_log`
  - `OpinionMockMasterDataProvider` 提供模拟主数据
- **前端任务概览**：
  - 统一 axios 实例 + API 封装
  - `@/constants/opinion.ts` 三栏映射
  - 公共组件：`StatusTag`、`RejectDialog`、`ConfirmDialog`、`AttachmentList`、`TabContainer`、`ProgressDialog`
- **验证方式**：单元测试覆盖状态机的 6 个主状态 + 4 套子状态的合法 / 非法转移
- **验收口径**：状态机用例全部通过；权限切面对 R02–R05 的越权请求返回 403
- **实现状态**：✅ 已完成

### STEP-003 P01 主链路（草稿 / 编辑 / 列表 / 开启 / 查看）

- **步骤目标**：实现绩效管理员的创建到开启动作链路
- **影响面**：[前端] [后端] [接口]
- **关联接口**：API-101、102、103、104、108、109
- **关联数据表**：`ad_opinion_survey/_module/_target/_attachment`
- **后端任务概览**：
  - `OpinionSurveyController` + `OpinionSurveyService`
  - 创建征集：写 survey + module + target + attachment
  - 开启动作：写 status=`WAIT_FILL` + 批量初始化 `ad_opinion_unit_task` + 通知打桩
  - 列表查询：返回含进度汇总的 survey 列表
- **前端任务概览**：
  - P01 列表页 + 编辑页 + 查看页 + 开启确认弹窗
- **验证方式**：创建草稿 → 编辑 → 开启，状态依次变为 DRAFT → DRAFT → WAIT_FILL
- **验收口径**：P01 列表可查、新增/编辑/删除/开启均可正常执行
- **实现状态**：✅ 已完成

### STEP-004 基层链路（P02 + P03 + 零报送）

- **步骤目标**：基层填报 → 基层审核闭环；零报送定时任务
- **影响面**：[前端] [后端] [接口]
- **关联接口**：API-201、202、203、204、301、302、303、304；API-110（绩效退回基层）
- **关联数据表**：`ad_opinion_unit_task/_item/_unit_audit_log/_action_log`
- **后端任务概览**：
  - `OpinionUnitFillController/Service`、`OpinionUnitAuditController/Service`
  - 提交先保存（API-203 → API-204）
  - 退回原因落 `ad_opinion_unit_audit_log`
  - `OpinionAutoZeroReportJob`：每 5 分钟扫描已过基层截止时间且 `fill_status='PENDING'` 的任务，执行零报送
- **前端任务概览**：
  - P02 列表页 + 填报详情页 + 提交确认弹窗
  - P03 列表页 + 审核详情页 + 审核/退回确认弹窗
- **验证方式**：
  - 基层填报 → 提交 → 审核通过 → 状态闭环
  - 基层审核退回 → 重新填报 → 审核通过 → 退回闭环
  - 基层截止时间到 → 未提交单位零报送自动通过
- **验收口径**：P02/P03 完整流程可走通；零报送定时任务按预期执行
- **实现状态**：✅ 已完成（API-110 绩效退回基层待实现）

### STEP-005 专业链路（P04 + P05 + 开启专业反馈）

- **步骤目标**：专业反馈 → 专业审核闭环；从基层完成推进到 `DEPT_FEEDBACK`
- **影响面**：[前端] [后端] [接口]
- **关联接口**：API-105、111、401–404、501–504
- **关联数据表**：`ad_opinion_dept_task/_feedback/_dept_audit_log`
- **后端任务概览**：
  - `OpinionDeptFeedbackController/Service`、`OpinionDeptAuditController/Service`
  - API-105「开启专业反馈」校验「全部基层已审核通过」
  - 按部门 ↔ 模块字典初始化 `ad_opinion_dept_task`，按 `item × dept_task` 初始化 `ad_opinion_feedback` 占位
  - API-111「绩效退回专业」校验主状态=`DEPT_FEEDBACK`
- **前端任务概览**：
  - P04 列表页 + 反馈详情页 + 提交确认弹窗
  - P05 列表页 + 审核详情页 + 审核/退回确认弹窗
- **验证方式**：专业退回闭环；绩效退回专业（API-111）；非「全部基层通过」状态调用 API-105 应被拦截
- **验收口径**：状态推进可逆性符合设计说明书 §5.3；接口契约一致
- **实现状态**：❌ 待实现

### STEP-006 进度查询与提醒（弹窗 + 一键 / 单条提醒）

- **步骤目标**：P01 进度弹窗与提醒能力
- **影响面**：[前端] [后端] [接口]
- **关联接口**：API-601、602、603、604
- **关联数据表**：`ad_opinion_reminder_log`
- **后端任务概览**：
  - `OpinionProgressController/Service`、`OpinionReminderController/Service`
  - 频控：5 分钟内同一目标不重复发通知，仍记录 `reminder_log` 但 `mode` 区分
  - 通知中心仍打桩
- **前端任务概览**：
  - P01 基层填报进度弹窗 + 专业反馈进度弹窗
  - 一键提醒 / 单条提醒按钮
- **验证方式**：进度弹窗排序、筛选、分页；一键提醒频控用例
- **验收口径**：弹窗规范符合需求 6.2 第 20 条；提醒结果可解释
- **实现状态**：❌ 待实现

### STEP-007 汇总发布（P01 汇总页 + 自动汇总推进 + 发布 + 模板导入 / 报表导出）

- **步骤目标**：从 `DEPT_FEEDBACK` 自动推进到 `DONE`；R01 在汇总页完成采纳决策与「调整后内容」录入；最终推进到 `PUBLISHED`
- **影响面**：[前端] [后端] [接口]
- **关联接口**：API-106、107、701、702、703、704
- **关联数据表**：`ad_opinion_summary_item`
- **后端任务概览**：
  - `OpinionSummaryController/Service`：从 `feedback` 初始化 `summary_item`；保存采纳决策与调整后内容
  - 状态机在「最后一个专业审核通过」时自动调用汇总动作
  - 报表导出：服务端流式生成 Excel
  - 模板导入：二段式（上传得 `fileId` → 解析校验 → 提交）
  - 发布动作必须校验「采纳=是」行的「采纳说明」全部非空
- **前端任务概览**：
  - P01 发布汇总页（含 5 个 Tab：关键业绩指标 / 业绩争取加分 / 党建工作指标 / 安全工作指标 / 其他）
  - 发布二次确认弹窗
- **验证方式**：汇总采纳页保存与发布；导入校验失败用例；P02/P05 已发布查看页透出「调整后内容」
- **验收口径**：发布后所有写接口返回 403 业务异常；导出 Excel 可在 Office 打开
- **实现状态**：❌ 待实现

### STEP-008 截止超时提醒（专业方）

- **步骤目标**：专业截止时间到达后向 R04 / R05 发送提醒（不自动提交）
- **影响面**：[后端]
- **关联接口**：无（定时任务）
- **关联数据表**：`ad_opinion_dept_task`、`ad_opinion_action_log`
- **后端任务概览**：
  - `OpinionDeptDeadlineRemindJob`：扫描临近截止 / 已过截止且未提交的部门，写 `action_log` + 调用通知打桩
- **前端任务概览**：无
- **验证方式**：把 `dept_deadline` 提前到当前时间，观察日志；把 `dept_deadline` 临近时观察提前提醒
- **验收口径**：仅提醒，不修改状态
- **实现状态**：⚠️ 部分完成 — 零报送部分（`OpinionAutoZeroReportJob`）已实现，专业截止提醒待实现

### STEP-009 导入 / 导出补完（P02–P05 各页面导出）

- **步骤目标**：API-801 / 802 数据导出补齐
- **影响面**：[前端] [后端] [接口]
- **关联接口**：API-801、802
- **关联数据表**：复用现有
- **后端任务概览**：复用 STEP-007 的 Excel 流式生成能力
- **前端任务概览**：P02/P03/P04/P05 详情页导出按钮
- **验证方式**：每个详情页导出可下载且字段口径与详情页一致
- **验收口径**：导出失败时给出明确错误
- **实现状态**：❌ 待实现

## 里程碑与依赖

| 里程碑 | 目标 | 前置依赖 | 完成标准 | 实现状态 |
| --- | --- | --- | --- | --- |
| M1 | 数据库 + 公共能力就绪 | — | STEP-001 + STEP-002 通过 | ✅ 已完成 |
| M2 | 绩效管理员主链路可用 | M1 | STEP-003 端到端通过 | ✅ 已完成 |
| M3 | 基层链路 + 零报送闭环 | M2 | STEP-004 通过 | ✅ 已完成 |
| M4 | 专业链路闭环 | M3 | STEP-005 通过 | ❌ 待实现 |
| M5 | 进度 + 提醒 | M3、M4 | STEP-006 通过 | ❌ 待实现 |
| M6 | 汇总发布与报表 | M4、M5 | STEP-007 通过 | ❌ 待实现 |
| M7 | 全模块联调验收 | M6、STEP-008、STEP-009 | 设计说明书 §10 验收用例全部通过 | ❌ 待实现 |

## 开发顺序

1. STEP-001 数据库基线 + 枚举 ✅
2. STEP-002 公共能力（状态机、权限、错误码、公共组件、前端三栏枚举） ✅
3. STEP-003 P01 主链路 ✅
4. STEP-004 基层链路 + 零报送 ✅
5. **STEP-005 专业链路** ← 下一优先级
6. STEP-006 进度 + 提醒
7. STEP-007 汇总发布
8. STEP-008 专业截止提醒
9. STEP-009 详情页数据导出补完
10. 最终联调：跑设计说明书 §10 全部 10 个验收用例

## 关键验证点

- 完整流程冒烟（STEP-007 后）
- 基层退回闭环（STEP-004）
- 专业退回闭环（STEP-005）
- 绩效退回基层（STEP-004 末）+ 绩效退回专业（STEP-005 末）
- 零报送（STEP-004 末）
- 一键提醒频控（STEP-006）
- 角色越权（每个 STEP 完成时跑一遍）
- 发布后只读（STEP-007 末）
- P02 / P05 已发布查看页透出「调整后内容」（STEP-007 末）
