# 项目初始化任务清单

> 本清单是项目初始化过程的执行锚点，每完成一个任务必须立即更新状态。

## 状态约定

- 待执行 / 执行中 / 已完成 / 已跳过（原因）

## 任务列表

### 一、信息收集与事实源

| 序号 | 任务 | 涉及文件/目录 | 状态 |
| --- | --- | --- | --- |
| 1.1 | 扫描脚手架占位符与本地产物 | `python .agents/skills/人工触发层/project-init/scripts/scan_init_targets.py` | 已完成 |
| 1.2 | 收集项目身份、技术基线、保留策略、运行事实 | 与开发者四批问答 | 已完成 |
| 1.3 | 生成项目初始化事实源 | `docs/项目信息/项目初始化信息.md` | 已完成 |
| 1.4 | 生成初始化任务清单 | `docs/项目信息/初始化任务清单.md` | 已完成 |

### 二、本地产物与历史清理

| 序号 | 任务 | 涉及文件/目录 | 状态 |
| --- | --- | --- | --- |
| 2.1 | 清理 `.opencode/node_modules` | `.opencode/node_modules/` | 已完成 |
| 2.2 | 把旧 docs 内容归档到 `docs/历史归档/项目初始化前-20260613202900/` | `docs/变更记录/`、`docs/需求/`、`docs/原型/`、`docs/原型站点/`、`docs/设计/`、`docs/工作台/` | 已完成 |
| 2.3 | 重建上述 docs 目录的最小骨架 | 同上 | 已完成 |
| 2.4 | 清理 `code/backend/package-lock.json` 孤儿文件 | `code/backend/package-lock.json` | 已完成（额外发现） |

### 三、命名替换（包根、模块名、项目名）

| 序号 | 任务 | 涉及文件/目录 | 状态 |
| --- | --- | --- | --- |
| 3.1 | 重命名后端业务模块物理目录 `ai-training-project/` → `aldemo/` | `code/backend/aldemo` | 已完成 |
| 3.2 | 重命名包路径物理目录 `com/hzzenith/ai/training/` → `com/hk/demo/`（4 个模块各自下面） | 各模块 `src/main/java`、`src/test/java` | 已完成 |
| 3.3 | 文本替换包根 `com.hzzenith.ai.training` → `com.hk.demo` | 全后端 | 已完成 |
| 3.4 | 文本替换 `ai-training-project` → `aldemo`（pom、yaml、文档） | 全仓库 | 已完成 |
| 3.5 | 文本替换 `process-ex` → `aldemo`（账号/密码/数据库/前端 title） | 全仓库 | 已完成 |
| 3.6 | 文本替换 `Process-Ex` → `公司培训项目`（前端 DEFAULT_TITLE 等可见处） | 前端 | 已完成 |
| 3.7 | 文本替换 `ai_drill_project` → `aldemo`（Nacos namespace） | 后端配置 | 已完成 |
| 3.8 | 修改前端折叠态短标识 `'PE'` → `'AL'` | `code/frontend/src/layout/Layout.vue`、`code/frontend/src/__tests__/layout.spec.ts`、`code/frontend/test-menu-clicks.cjs` | 已完成 |
| 3.9 | 重命名 Nacos 配置文件 `ai-training-project.yaml` → `aldemo.yaml` | `code/backend/aldemo/src/main/resources/nacos/` | 已完成 |
| 3.10 | 重命名启动类 `AiTrainingProjectApplication` → `AldemoApplication`（含同名测试类） | `code/backend/aldemo/src/main/java/...`、`src/test/java/...` | 已完成 |
| 3.11 | 后端 pom 顶层 `groupId/artifactId/description/name` 同步 | `code/backend/pom.xml` 与各子模块 pom | 已完成 |
| 3.12 | 前端 `package.json` 中 `name` → `aldemo` | `code/frontend/package.json` | 已完成 |
| 3.13 | 前端 `index.html` `<title>` 与路由 `DEFAULT_TITLE` 改为 `公司培训项目` | `code/frontend/index.html`、`code/frontend/src/router/index.ts` | 已完成 |
| 3.14 | 清理 backend-data `@ConfigurationProperties` 前缀 `hzzenith.ai.training.data` → `hk.demo.data` | `DataAccessProperties.java` | 已完成 |
| 3.15 | 清理 OpenAPI 标题/描述/contact | `OpenApiConfig.java` | 已完成 |
| 3.16 | 清理 `application-test.yaml`、`application-oracle.yaml` 中 `training` 占位 | 同 | 已完成 |
| 3.17 | 剥离批量替换写入的 UTF-8 BOM | 39 个被替换的 Java/YAML/MD/POM 文件 | 已完成 |

### 四、配置事实更新

| 序号 | 任务 | 涉及文件/目录 | 状态 |
| --- | --- | --- | --- |
| 4.1 | 更新 `application.yaml` 中 `name`、`namespace`、`type-aliases-package`、数据库名 | `code/backend/aldemo/src/main/resources/application.yaml` | 已完成 |
| 4.2 | 更新 `application-mysql.yaml` 中 jdbc URL 占位、用户名、密码 | 同上 | 已完成 |
| 4.3 | 父 POM `pluginManagement` 中为 `spring-boot-maven-plugin` 添加 `<skip>true</skip>` | `code/backend/pom.xml` | 已完成 |
| 4.4 | 业务应用模块 pom 显式声明 `spring-boot-maven-plugin`（覆盖跳过） | `code/backend/aldemo/pom.xml` | 已完成 |
| 4.5 | 修改 `StaticResourceController` 增加根路径欢迎页 | `code/backend/aldemo/src/main/java/com/hk/demo/app/controller/StaticResourceController.java` | 已完成 |
| 4.6 | 重置 `code/backend/DataSQL/业务变更汇总清单.md` 为初始骨架 | 同 | 已完成 |
| 4.7 | Nacos 配置 prod profile 中数据库名 `${APP_DATABASE_NAME:training}` → `${APP_DATABASE_NAME:aldemo}` | `aldemo.yaml` | 已完成 |

### 五、文档同步

| 序号 | 任务 | 涉及文件/目录 | 状态 |
| --- | --- | --- | --- |
| 5.1 | 同步根 `AGENTS.md`（项目结构示例段落） | `AGENTS.md` | 已完成 |
| 5.2 | 同步 `code/backend/AGENTS.md`（包根、模块名、运行命令） | `code/backend/AGENTS.md` | 已完成 |
| 5.3 | 同步 `code/frontend/AGENTS.md`（项目代号、标题） | `code/frontend/AGENTS.md` | 已完成 |
| 5.4 | 同步 `docs/规范/AI开发SOP.md` 与 `docs/规范/数据库留痕规范.md` 中的项目事实 | `docs/规范/` | 已完成 |
| 5.5 | 同步 `.agents/skills/默认执行层/backend-development-standard/SKILL.md` 等保护资产中的项目事实占位（仅事实，不改规则本体） | `.agents/skills/` 与 `.agents/description/` | 已完成 |

### 六、启动脚本

| 序号 | 任务 | 涉及文件/目录 | 状态 |
| --- | --- | --- | --- |
| 6.1 | 生成 `script/start-backend.sh` 与 `script/start-backend.cmd` | `script/` | 已完成 |
| 6.2 | 生成 `script/start-frontend.sh` 与 `script/start-frontend.cmd` | `script/` | 已完成 |
| 6.3 | 生成 `script/start-all.sh` 与 `script/start-all.cmd` | `script/` | 已完成 |
| 6.4 | 验证启动脚本可正常启动 | `script/start-backend.cmd`、`script/start-frontend.cmd` | 已完成（前端已成功启动 Vite；后端已成功编译并启动 Spring Boot 上下文，仅因外部 MySQL `aldemo` 账号未创建而拒登录，属预期人工补充项） |

### 七、验证与留痕

| 序号 | 任务 | 涉及文件/目录 | 状态 |
| --- | --- | --- | --- |
| 7.1 | `./mvnw -q -N help:effective-pom` | `code/backend/` | 已完成（OK） |
| 7.2 | `./mvnw -pl aldemo -am -DskipTests validate` | `code/backend/` | 已完成（5/5 模块 SUCCESS） |
| 7.3 | `./mvnw -pl aldemo -am -DskipTests dependency:tree` | `code/backend/` | 已完成（依赖树解析成功） |
| 7.4 | 更新 `docs/变更记录/CHANGELOG.md` | `docs/变更记录/CHANGELOG.md` | 已完成 |
| 7.5 | 更新 `docs/变更记录/规范变更.md` 与 `架构变更.md` | 同 | 已完成 |
| 7.6 | 关闭本任务清单，记录完成时间与剩余待人工补充项 | 本文件 | 已完成 |

## 完成结论

- 完成时间：2026-06-13 20:46（下午）
- 自动化执行结论：项目初始化已完成。Maven 多模块编译、依赖解析、Spring Boot 上下文启动均已通过；前端 Vite 启动已通过。
- 剩余待人工补充项：
  1. 在 MySQL（默认 `118.25.114.174:3306` 或本地）中创建数据库 `aldemo` 与同名账号、授权，或在启动前导出 `MYSQL_URL` / `MYSQL_USERNAME` / `MYSQL_PASSWORD` 环境变量
  2. 准备本地 / 测试环境 Nacos：默认 `127.0.0.1:8848`、namespace `aldemo`、group `nacos`、dataId `aldemo.yaml`（首次启动可临时关闭 Nacos discovery/config 或在 dev profile 中跳过）
  3. 后续业务接入时，先在 `docs/需求/模块总表.md` 登记模块编码，再在 `code/backend/aldemo/` 与 `code/backend/DataSQL/` 下按规范建立模块子目录
  4. 视需要把 `code/backend/CHANGELOG.md`（后端模块级变更日志）作为正式留痕入口启用
