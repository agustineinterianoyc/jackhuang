# 项目初始化信息

> 本文件由 `project-init` 流程生成，是当前项目的初始化事实源。

## 项目身份

- 中文名：公司培训项目
- 英文名：aldemo
- 简称 / 代号：aldemo
- 后端 Java 包根：`com.hk.demo`
- 业务应用模块名：`aldemo`
- 业务应用物理目录：`code/backend/aldemo/`（与 artifactId、Nacos dataId、spring.application.name 完全一致）
- 共享模块名：`backend-api`、`backend-core`、`backend-data`（沿用模板）

## 技术基线

- JDK：21（父 POM 通过 maven-enforcer 强制 `[21,22)`）
- Maven：沿用 wrapper（`mvnw` / `mvnw.cmd`）
- Spring Boot：4.0.6
- Spring Cloud：2025.1.0
- Spring Cloud Alibaba：2025.1.0.0
- MyBatis-Plus：3.5.15（`mybatis-plus-spring-boot4-starter`）
- Springdoc：3.0.3
- Knife4j：4.5.0
- Node：`^20.19.0 || >=22.12.0`（沿用前端 `package.json` engines 字段）
- 默认数据库：MySQL
- 保留数据库 profile：`mysql`、`oracle`、`postgresql`（默认仅启用 `mysql`，其他作为扩展位）

## 初始化策略

- 共享模块：保留 `backend-api/backend-core/backend-data` 现名
- 后端业务残留：保留 `AldemoApplication`（重命名自 `AiTrainingProjectApplication`）与 `StaticResourceController` 作为最小启动骨架
- 静态首页：改写为项目欢迎页（公司培训项目）
- DataSQL：`code/backend/DataSQL/业务变更汇总清单.md` 重置为初始骨架，等待真实业务模块加入
- docs 历史：`docs/变更记录/`、`docs/需求/`、`docs/原型/`、`docs/原型站点/`、`docs/设计/`、`docs/工作台/` 归档到 `docs/历史归档/项目初始化前-{时间戳}/`，再重建最小骨架
- 本地产物：清理 `.opencode/node_modules`

## 运行与命名事实

- `spring.application.name`：`aldemo`
- Nacos namespace：`aldemo`（替换原 `ai_drill_project`）
- Nacos dataId：`aldemo.yaml`
- 数据库名：`aldemo`
- MySQL 默认账号 / 密码占位词：`aldemo`（仍需通过环境变量 `MYSQL_USERNAME` / `MYSQL_PASSWORD` 覆盖）
- 表名前缀：`ad_`
- 默认环境 profile：`dev,test,prod`，启动时默认 `dev,mysql`
- 前端 `<title>` 与 `DEFAULT_TITLE`：`公司培训项目`
- 前端 `package.json` 的 `name`：`aldemo`
- 前端 API 前缀：维持模板默认（如有变化以 `code/frontend/` 实际事实为准）

## 文档与留痕

- 项目初始化事实源：本文件
- 初始化任务执行锚点：`docs/项目信息/初始化任务清单.md`
- 项目级变更事实源：`docs/变更记录/`
- 后端约束事实：`code/backend/AGENTS.md`
- 前端约束事实：`code/frontend/AGENTS.md`
- 数据库变更留痕：`code/backend/DataSQL/`

## 模板规则保护边界

下列资产在本次初始化中只做项目事实替换，不修改规则本体：

- `.agents/skills/`
- `.agents/description/`
- `docs/规范/AI开发SOP.md`
- `docs/规范/`
